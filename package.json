{
  "name": "glukvpn",
  "version": "0.1.0",
  "private": true,
  "description": "GlukVPN — self-hosted WireGuard VPN prototype (control plane + node agent + Flutter client)",
  "license": "UNLICENSED",
  "workspaces": [
    "control-server",
    "node-agent"
  ],
  "scripts": {
    "build": "npm run build --workspaces --if-present",
    "test": "npm run test --workspaces --if-present",
    "typecheck": "npm run typecheck --workspaces --if-present",
    "lint": "npm run typecheck --workspaces --if-present"
  },
  "engines": {
    "node": ">=20.11.0"
  }
}
